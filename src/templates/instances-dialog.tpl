<% if (typeof(list) !== 'undefined') { %>
	<div class="streams">
		<p class="streams-heading">Инстансы:</p>
		<ul class="streams-list"></ul>
	</div>
<% } else { %>
	<p class="streams-heading">Инстансов нет.</p>
<% } %>
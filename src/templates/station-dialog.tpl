<% if (typeof(list) !== 'undefined') { %>
	<div class="streams">
		<p class="streams-heading">Перелёты:</p>
		<ul class="streams-list"></ul>
	</div>
<% } else { %>
	<p class="streams-heading">Перелётов нет.</p>
<% } %>